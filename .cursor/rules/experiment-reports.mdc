---
description: 项目内报告的统一结构。适用于 docs 与 research 下的 Markdown。
globs: docs/**/*.md,research/**/*.md
alwaysApply: false
---

# 报告结构

何时撰写实验报告、放在哪一目录，以 `experiment-management.mdc` 为准。本规则约定全项目报告怎么组织。

## 结构

使用 Markdown。公式只用 `$...$` 和 `$$...$$`。

文首一段写明：本文回答什么、材料或结果从哪来、日期、不覆盖什么。

正文按这个顺序：

1. **范围**：问题、对象、对照，以及明确不做的部分。
2. **来源**：文献、数据、代码或配置。可核对的写成引用键或项目相对路径。
3. **结果**：先给结论，再用表格比较。每个数字写明样本数、对照和出处。
4. **边界**：不确定性、不能外推的条件、尚未测量的部分。负结果和证据不足保持原状。

一节只回答一个问题。节标题写成问题或判断。

图片与引用它的正文放在同一目录，用相对路径引用。完整 prompt、完整预测文本、KV 和激活不写入报告；放不下的实体只写项目相对路径和校验值。

正式实验报告的入口文件为该报告目录下的 `index.md`。文首 YAML 至少包含 `experiment`、`results`、`reviewed`。`results` 是当前结果目录的项目相对路径，`reviewed` 为 `true`。

```markdown
---
experiment: <实验名>
results: research/phaseN/results/<实验名>
reviewed: true
---
```
