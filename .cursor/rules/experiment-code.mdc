---
description: 项目内代码与注释的统一格式。适用于 Python。
globs: "**/*.py"
alwaysApply: false
---

# 代码与注释

目录、阶段边界和结果回传以 `experiment-management.mdc` 为准。本规则约定全项目代码怎么写。

## 代码

- Python 3.11，UTF-8，4 空格缩进，行宽不超过 100。
- 模块、函数、变量用 snake_case；类用 PascalCase；常量用 UPPER_SNAKE。
- 公共函数标注参数与返回类型。导入按标准库、第三方、本项目分组，组间空一行。
- 路径从项目根解析。配置和代码中的资源使用项目相对路径。
- 一个函数做一件事。单位、副作用和失败原因放在签名附近能看见的地方。
- 异常留下原因。资源耗尽、超时和评分失败记为失败。

## 注释

- 注释和文档字符串使用中文。
- 模块、公共类、公共函数写文档字符串：第一行说明用途；参数、返回值、单位或副作用不显而易见时再补一行。
- 行内注释只解释约束、单位、假设或不变量。
- 不写作者、日期和变更记录，不保留调试打印和注释掉的旧实现。

```python
def score_batch(tokens: int, elapsed_s: float) -> float:
    """按每秒处理的 token 数计算吞吐。elapsed_s 为零时返回 0。"""
    if elapsed_s <= 0:
        return 0.0
    return tokens / elapsed_s
```
