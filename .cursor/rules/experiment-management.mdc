---
description: 研究目录、跨阶段共用代码、阶段执行代码、结果回传与审核后报告的约定
alwaysApply: true
---

# 研究目录

## 布局

- 研究工作放在项目根目录的 `research/`，与 `docs/` 并列。`docs/` 保存课题范围、文献和阅读资料。现有文件的迁移随相应整理任务执行，不因读取本规则自动移动。
- `research/src/kvstudy/` 只放各阶段共用的代码。第 N 阶段的执行代码放在 `research/phaseN/src/phaseN/`。阶段二、三在需要时按同一形状建立，不复制上一阶段的执行代码。
- 共用代码不得导入任何 `phaseN`。阶段执行代码可以导入 `kvstudy`。后续阶段要复用的逻辑先提取到 `research/src/kvstudy/`，并补上回归测试。
- 每个阶段的指导文档是 `research/phaseN/guide.md`，只写要回答的问题、实验顺序，以及每步的做法、记录和通过条件。
- 模型、数据、量化、判定和软件等默认值放在该阶段的 `configs/`，按主题分文件。配置不安排实验顺序，不写报告结构。同一决定只写在一处；指导文档引用配置，不另抄一份。

## 结果与报告

- 正式实验的运行过程不写报告，也不生成图片。
- 运行结束后整理汇总指标、小型逐样本评分和来源索引，回传到本地 `research/phaseN/results/<实验名>/`。同一实验修改后再次运行，用新结果整目录覆盖该目录里的旧结果。
- 完整 prompt、完整预测文本、KV 和激活默认不回传。单文件超过 10 MiB 的实体留在服务器，结果索引只写项目相对路径和校验值。
- 回传结果先经审核。审核通过后才生成图片，并按 `experiment-reports.mdc` 撰写 `research/phaseN/reports/<实验名>/`。结果回传不得改写 `reports/`。同一实验再次审核后，新报告替换该实验的旧报告。
- 一次性测试的代码和结果只可写在服务器 `research/.runtime/scratch/`。测试结束后删除，不回传，不放入 `phaseN/`，不进入 Git。

## 文件归属

- 本地 `F:\phd-research` 维护代码、规则、配置、指导文档、当前正式结果和审核后的报告。服务器 `/cluster/home/zengy/phd-research` 执行实验。访问范围仍受 `workspace-isolation.mdc` 约束。
- 纳入 Git：规则、`docs/`、共用代码与其长期测试、各阶段执行代码、指导文档、配置、样本划分、环境锁定文件、来源清单、当前正式结果、审核后的报告。
- 不纳入 Git：根目录 `models/`，以及服务器 `research/.runtime/`（运行环境、缓存、临时文件、一次性测试）。
- 资源和输出路径从项目根解析。配置使用项目相对路径。
- 代码发布、结果回传和 GitHub 同步是独立操作。明确要求云同步时，按 `AGENTS.md` 执行。服务器连接使用 `docs/server-access.md` 的 `ssh` 命令，每次先进入服务器项目根目录。不新增连接脚本，不复制或读取私钥。
- 代码与注释格式见 `experiment-code.mdc`。报告结构见 `experiment-reports.mdc`。
